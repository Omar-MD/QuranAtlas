<!--
  Synthetic empty route component. Returned by the #/surahs loader on mobile
  when redirecting to the drawer; never user-visible (the redirect runs in a
  microtask before the component would paint). Don't import this elsewhere.
-->
<script lang="ts">
  // intentionally empty
</script>
