# Phase 0: Foundation | Blocks all Phase 1–3 stories

- `package.json`: type:module, engines.node>=22; `.nvmrc`: Node 22; `npm install` → zero high/critical audit vulns
- `vite.config.js`: entry `src/core/app.js`; injectManifest; manualChunks → 6 chunks (shell, reader, dataset, marks-review, settings, vendor); `css.transformer:'lightningcss'` chrome:112; `build.modulePreload.polyfill:false`; `build.assetsInlineLimit:0`; `npm run build` → no inline `<script>` in index.html; no chunk >150 KB gzip (post-build check)
- `eslint.config.js` (flat): no-eval, no-implied-eval, no-new-func, no-restricted-globals (localStorage, sessionStorage), no-unsanitized, unicorn selective, prettier; `npm run lint` → zero errors/warnings
- `vitest.config.js`: jsdom, fake-indexeddb setup file, v8 coverage (lines 80%, functions 80%); `playwright.config.js`: Chromium, baseURL=vite preview; `lighthouserc.json`: PWA≥80, Perf≥80, A11y≥90, BestPractice≥85; `lefthook.yml`: pre-commit lint+format staged JS; `lefthook install` succeeds
- `src/sw.js`: `precacheAndRoute(__WB_MANIFEST)`; CacheFirst `/fonts/**`; CacheFirst `/dataset/**`; `SKIP_WAITING` handler; SW registers + serves fonts from cache after first load
- `core/events.js`: emit/on/off; `core/db.js`: openDB schema v1 (marks+by-tag+by-updated, positions, settings, datasetMeta, activationState) + versionchange handler; `core/router.js`: hash routing + `dynamic import()`; `core/app.js`: entry init; `core/error.js`: window.onerror + unhandledrejection + reportError()
- `scripts/build-dataset.js`: quran.com API primary, quran-json fallback → per-surah JSON + surahs.json + juz.json + annotations.json + provenance.json + manifest.json (SHA-256); output to `public/dataset/`; verify exactly 6,236 ayahs across 114 surahs; provenance.json documents CC BY-NC-ND 4.0 constraint
- `.github/workflows/ci.yml`: lint → unit (coverage) → build (chunk size check) → e2e + lighthouse (parallel) → deploy Cloudflare Pages (main only)
- `index.html`: minimal shell, `<meta>` CSP, `<link rel="preload">` KFGQPC font, viewport without user-scalable=no, `<link rel="manifest">`; no inline `<script>`; `public/_headers`: CSP + X-Content-Type-Options + X-Frame-Options + Referrer-Policy + Permissions-Policy + Cache-Control rules
